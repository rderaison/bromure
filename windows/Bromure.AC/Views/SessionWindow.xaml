<Window x:Class="Bromure.AC.Views.SessionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        Title="Bromure AC"
        Width="1280" Height="800"
        Background="#0d1117"
        Foreground="{StaticResource TextPrimary}"
        WindowStartupLocation="CenterScreen"
        SizeToContent="Manual"
        ResizeMode="CanResize">
    <!-- Custom title bar — Windows Terminal style. We draw the
         min/max/close buttons ourselves on the right; tabs +
         new-tab (+) sit in the draggable caption area. Caption
         clicks drag the window; tab + button clicks pass through
         via IsHitTestVisibleInChrome. -->
    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome
            CaptionHeight="38"
            CornerRadius="0"
            GlassFrameThickness="0"
            ResizeBorderThickness="6"
            UseAeroCaptionButtons="False"/>
    </shell:WindowChrome.WindowChrome>
    <Grid Background="#0d1117">
        <Grid.RowDefinitions>
            <RowDefinition Height="38"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <!-- Title bar: tabs (left, with + button after) | window controls (right) -->
        <Grid Grid.Row="0" Background="#0d1117">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0"
                        Orientation="Horizontal"
                        Margin="8,4,0,0"
                        VerticalAlignment="Bottom"
                        shell:WindowChrome.IsHitTestVisibleInChrome="True">
                <StackPanel x:Name="TabStrip" Orientation="Horizontal"/>
                <Button x:Name="NewTabButton"
                        Content="+"
                        Click="OnNewTabClick"
                        Width="32" Height="32"
                        Margin="6,0,0,2"
                        Padding="0"
                        BorderThickness="0"
                        Background="Transparent"
                        Foreground="#BFD0E6"
                        FontSize="18"
                        FontWeight="Light"
                        Cursor="Hand"
                        ToolTip="New tab (Ctrl+T)"
                        VerticalAlignment="Bottom"
                        shell:WindowChrome.IsHitTestVisibleInChrome="True"/>
            </StackPanel>
            <StackPanel Grid.Column="1"
                        Orientation="Horizontal"
                        VerticalAlignment="Top"
                        shell:WindowChrome.IsHitTestVisibleInChrome="True">
                <Button x:Name="MinButton"
                        Click="OnMinClick"
                        Content="&#xE921;"
                        FontFamily="Segoe MDL2 Assets"
                        Width="46" Height="38"
                        Background="Transparent"
                        Foreground="#E6EDF3"
                        BorderThickness="0"
                        FontSize="10"
                        ToolTip="Minimize"/>
                <Button x:Name="MaxButton"
                        Click="OnMaxClick"
                        Content="&#xE922;"
                        FontFamily="Segoe MDL2 Assets"
                        Width="46" Height="38"
                        Background="Transparent"
                        Foreground="#E6EDF3"
                        BorderThickness="0"
                        FontSize="10"
                        ToolTip="Maximize"/>
                <Button x:Name="CloseButton"
                        Click="OnCloseClick"
                        Content="&#xE8BB;"
                        FontFamily="Segoe MDL2 Assets"
                        Width="46" Height="38"
                        Background="Transparent"
                        Foreground="#E6EDF3"
                        BorderThickness="0"
                        FontSize="10"
                        ToolTip="Close"/>
            </StackPanel>
        </Grid>
        <ContentPresenter Grid.Row="1" x:Name="ActiveContent"/>
    </Grid>
</Window>
