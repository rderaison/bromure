<UserControl x:Class="Bromure.AC.Views.InitializingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{StaticResource ChromeBackground}"
             Foreground="{StaticResource TextPrimary}">
    <Grid Margin="32,32" MaxWidth="780" HorizontalAlignment="Center">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" VerticalAlignment="Center"
                    Margin="0,0,0,24">
            <Border Width="48" Height="48" CornerRadius="10"
                    Background="{StaticResource AccentBrush}" Margin="0,0,16,0">
                <TextBlock Text="b" FontSize="28" FontWeight="Bold"
                           HorizontalAlignment="Center" VerticalAlignment="Center"
                           Foreground="{StaticResource TextPrimary}"/>
            </Border>
            <StackPanel VerticalAlignment="Center">
                <TextBlock Text="Building base image" FontSize="16" FontWeight="SemiBold"/>
                <TextBlock Text="This is the one-time install. Don't close the window."
                           FontSize="11" Foreground="{StaticResource TextSecondary}"/>
            </StackPanel>
        </StackPanel>

        <!-- Status + progress card -->
        <Border Grid.Row="1" Background="{StaticResource PanelBackground}"
                CornerRadius="6" Padding="16,12">
            <StackPanel>
                <Grid Margin="0,0,0,8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="{Binding Status}"
                               TextWrapping="Wrap" TextTrimming="CharacterEllipsis"
                               Foreground="{StaticResource TextPrimary}"/>
                    <TextBlock Grid.Column="1" Text="{Binding ProgressPercent, Mode=OneWay}"
                               FontFamily="Consolas"
                               Foreground="{StaticResource TextSecondary}"/>
                </Grid>
                <ProgressBar Height="6" Minimum="0" Maximum="1.0"
                             Value="{Binding Progress, Mode=OneWay}"
                             Foreground="{StaticResource AccentBrush}"
                             Background="#22FFFFFF" BorderThickness="0"/>
            </StackPanel>
        </Border>

        <!-- Console -->
        <Expander Grid.Row="2" Header="Console output" IsExpanded="True"
                  Foreground="{StaticResource TextSecondary}" Margin="0,16,0,0">
            <Border Background="#000000" CornerRadius="6" Padding="12">
                <ScrollViewer x:Name="ConsoleScroll" VerticalScrollBarVisibility="Auto"
                              HorizontalScrollBarVisibility="Disabled" Height="280">
                    <TextBlock Text="{Binding ConsoleLog}" FontFamily="Consolas"
                               FontSize="11" Foreground="#C9D1D9" TextWrapping="NoWrap"/>
                </ScrollViewer>
            </Border>
        </Expander>

        <!-- Error / cancel -->
        <Grid Grid.Row="3" Margin="0,16,0,0" Visibility="{Binding HasError, Converter={StaticResource BoolToVis}}">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <TextBlock Text="{Binding Error}" Foreground="{StaticResource DangerBrush}"
                           VerticalAlignment="Center" Margin="0,0,16,0"/>
                <Button Content="Close" Width="100" Height="32"
                        Command="{Binding DataContext.CancelCommand,
                                   RelativeSource={RelativeSource AncestorType=Window}}"/>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
