{
  "//": "Bromure AC plan-driver deps. agentd copies this to ~/.bromure/plan-driver/package.json in the guest and runs `npm install --no-audit --no-fund` there on first claude plan-stream launch. @anthropic-ai/claude-agent-sdk is version-pinned: 0.3.214 was the latest on npm as of 2026-07-20 (bump deliberately — the driver's canUseTool/AskUserQuestion contract must be re-verified against release notes).",
  "name": "bromure-plan-driver",
  "private": true,
  "type": "module",
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "0.3.214"
  }
}
